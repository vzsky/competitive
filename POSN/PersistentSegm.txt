Objective : Store every version of updated tree

Naive : Copy and store every tree  
n times, 

Problem : Kth - number 
    100,000 number
    100,000 Query of a, b, k answer kth minimum number in a, b
    a, b < 100,000

1 --- 2 --- 4
|     `---- 5
`---- 3 --- 6
      `---- 7

once update a node, log(n) (level) 
next versions extends by adding log(n) nodes to the base version.
* for implementation
    keep segment tree as array of left, right, val and keep root node of each version