sol1 ← {(⊂⍨)∘((×⍺-1)=⍺∘|)∘(⍳≢)⍨⍵}

sol2 ← {'FDCBA'[1+(65,10×6+⍳3)⍸⍵]}
sol2 ← 'FDCBA'(⊃⍤0 1)⍨(1+(65,10×6+⍳3)⍸⊢)

sol3 ← ('ABCDF',⊢,1(⍎⍕)¨⊢×100÷+⌿)⍤⍪(+/'ABCDF'∘.=⊢)

sol4 ← /⍨∘(∧/∘(⊣∊∘(⍳8))¨)⍨ ↓⍤(+⍤1)∘(8 2⍴(-,⊢)(⌽,⊢)(1 2 ¯1 2))

sol5 ← (∨/2=/' ',⊢)¨⊆

sol6 ← (('123456789ADGJMPTW'⍸⊢)-(8×∊∘⎕A))

sol7 ← ↑{(-⍺)↑⍵↑⍨((⌊∘⍺)(≢⍵)+(⌈)(÷∘2)(⍺-(≢⍵)))}¨∘⊆

sol8 ← +/*∘.5∘(+/¨)⍤×⍨∘(2-/⊢)∘(⊢,⊂∘⊃)

sol9 ← (÷∘2)∘|∘((1 0∘⊖) -⍥((+/)∘(×/)) (0 1∘⊖))⍤↑

sol10 ← 1∘↓¨⍤((⊂⊢)⌸)⍨∘((2|≢)¨)⍨ ('.' ''∘,)
